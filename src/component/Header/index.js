function Header() {
    return (
        <header>
            <div classNames="container">
                <div classNames="header-logo">
                    <img src="./img/Logo-trang.svg" alt="logo" classNames="main-logo" />
                </div>
                <div classNames="header-action">
                    <button classNames="btn">Đăng nhập</button>
                </div>
            </div>
        </header>
    );
}

export default Header;